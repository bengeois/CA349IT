import React from 'react';

const Space = (props) => {
    return (
        <div
            style={{
                minHeight: props.value
            }}
        >
        </div>
    );
};

export default Space;